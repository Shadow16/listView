<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nestor.imageslistview" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".GuitarHero"></activity>
        <activity android:name=".AmorFisica"></activity>
        <activity android:name=".Bioshock"></activity>
        <activity android:name=".CienAnios"></activity>
        <activity android:name=".HeroePerdido"></activity>
        <activity android:name=".Principito"></activity>
        <activity android:name=".SeduccionPal"></activity>
        <activity android:name=".TrampaOso"></activity>
        <activity android:name=".Valores"></activity>
        <activity android:name=".VeintePetalos"></activity>

    </application>

</manifest>
